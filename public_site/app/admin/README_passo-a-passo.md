## Configurar Firebase (ADMIN)

1) No Firebase Console, crie um App Web e copie o Config (apiKey, authDomain, ...).
2) Copie o arquivo .env.local.example para .env.local e cole os valores (sem aspas).
3) Reinicie o 'npm run dev' após criar o .env.local.
